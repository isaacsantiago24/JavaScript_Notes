//isArray checks if the past value is an array
//will return true or false

const names = ["florin", "ivan", "Liam"];
const str = "Hello World";
const number = 17;

console.log(Array.isArray(names));
